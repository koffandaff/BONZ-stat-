// Lenis removed — native CSS scroll-behavior: smooth handles everything
// This stub exists so imports don't break
export function initLenis() {
  // No-op: native scrolling is instant and GPU-accelerated
  return () => {}
}
